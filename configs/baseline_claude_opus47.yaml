provider: bedrock
model: global.anthropic.claude-opus-4-7
temperature: 1.0
max_tokens: 4096
concurrency: 8

# MDP hyperparameters
horizon: 6
top_k: 10
alpha: 0.1
beta: 1.0

# Paths
cache_dir: cache/search
results_dir: results
